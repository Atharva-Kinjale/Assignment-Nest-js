import { TransformResponceInterceptor } from './transform-responce.interceptor';

describe('TransformResponceInterceptor', () => {
  it('should be defined', () => {
    expect(new TransformResponceInterceptor()).toBeDefined();
  });
});
